#include "fileT.h"
void enfiler(File *file, int val){

    elementF *fl = malloc(sizeof(*fl));
    if(file == NULL || file == NULL){
        exit();
    }
    fl->value = val;
    fl->next = NULL;




}
